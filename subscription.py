

class Subscription(object):
    """
        A subscription is a logical container for events. 
        For example, a door has a subscription. And the
        events might be open, close, error, ...
    """
